class Solution {
    public int hammingDistance(int x, int y) {
        int count=0;
        String s=Integer.toBinaryString(x^y);
        for (int i=0; i<s.length(); i++) {
            if (s.charAt(i)=='1')
                count++;
        }
        return count;
    }
}
